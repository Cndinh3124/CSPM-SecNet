policies:
  - name: cspm-ssh-public-remediation
    resource: security-group
    description: Remediate public SSH access on CSPM test Security Group

    filters:
      - GroupId: sg-0704831de03c22a54
      - type: ingress
        IpProtocol: tcp
        FromPort: 22
        ToPort: 22
        Cidr: 0.0.0.0/0

    actions:
      - type: remove-permissions
        ingress: matched